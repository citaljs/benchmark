{
  "name": "@benchmark/synthesizer-base",
  "private": true,
  "version": "0.0.0",
  "main": "dist/index.js",
  "module": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "rimraf dist && node build.mjs && tsc"
  },
  "devDependencies": {
    "esbuild": "^0.19.11",
    "rimraf": "^5.0.5",
    "typescript": "^5.2.2"
  }
}
