{
  "name": "engine",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "rimraf dist && node build.mjs && tsc"
  },
  "dependencies": {
    "@architecture-benchmark/store-ts": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.10.6",
    "esbuild": "^0.19.11",
    "rimraf": "^5.0.5",
    "typescript": "^5.2.2"
  }
}
